/**
 * Public exports for shared token primitives.
 *
 * This module must not import from the token domain module.
 * It provides shared constants to prevent circular dependencies.
 */
export * from './token.constants'
